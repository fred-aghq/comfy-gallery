# Path to your ComfyUI output directory on the host
MEDIA_ROOT=/path/to/ComfyUI/output

# PostgreSQL password (change for production)
POSTGRES_PASSWORD=gallery

# Port to expose the frontend in development (default: 5173)
FRONTEND_PORT=5173

# Port to expose the frontend in production (default: 80)
PORT=80
